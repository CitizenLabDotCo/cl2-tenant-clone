# cl2-tenant-clone

Service for cloning CitizenLab tenants.

## Setup

1. Copy environment variables:
   ```bash
   cp .env-sample .env
   ```

2. Update `.env` with your database credentials

3. Run with Docker:
   ```bash
   docker compose up
   ```
